import './index.scss';

const Authentication = () => {
    return (
        <section className='auth-wrapper'>
            Authentication


        </section>
    );
};

export default Authentication;